# Arcadium Yota comparison extraction request

This branch exists only to run the oversized-source extraction workflow and produce a structured comparison source. The original `arcadium_yota.txt` remains unchanged.
