name: Archived Arcadium Yota extraction helper

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Archived analysis helper
        run: echo 'The one-time Arcadium comparison extraction is complete. Canon and manuscript workflows are unaffected.'
